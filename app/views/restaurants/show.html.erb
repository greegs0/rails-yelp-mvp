<div class="container">
  <h1><%= @restaurant.name %></h1>
  <p><strong>Address:</strong> <%= @restaurant.address %></p>
  <p><strong>Phone:</strong> <%= @restaurant.phone_number %></p>
  <p><strong>Category:</strong> <%= @restaurant.category %></p>

  <h2>Reviews</h2>
<ul class="list-group mb-3">
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item">
      <div class="d-flex justify-content-between align-items-center w-100">
        <p class="mb-0 flex-grow-1">
          <strong><%= review.rating %>/5</strong> — <%= review.content %>
        </p>
        <%= button_to "🗑️", review_path(review),
            method: :delete,
            data: { turbo_confirm: "Supprimer cet avis ?" },
            class: "btn btn-sm border" %>
      </div>
    </li>
  <% end %>
</ul>


  <h3>Add a review</h3>
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit "Submit review", class: "btn btn-sm btn-outline-primary" %>
  <% end %>

  <%= link_to "Retour",  restaurants_path, class: "btn btn-primary mb-3 mt-4" %>
</div>
